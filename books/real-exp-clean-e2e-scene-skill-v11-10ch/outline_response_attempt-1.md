不能生成 Outline，已按批准门禁停止。

核对到的实际状态：

- Fantasy Seed：`origin=model_selected`
- World Vision：`origin=model_generated`
- Story Program：`origin=model_generated`，且 `WORKFLOW_STATE.json` 仍为 `DRAFT`

根据当前规则，`author_approved` 状态不能覆盖模型来源；这三份产物尚未能被确认是作者明确批准的版本。未修改任何文件。请先通过明确的作者批准动作持久化状态，再重新生成 Outline。



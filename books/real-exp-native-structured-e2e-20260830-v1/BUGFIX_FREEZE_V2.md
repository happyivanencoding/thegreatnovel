# Native Structured E2E v2｜Pre-registered Bugfix Freeze

The first full runs exposed a deterministic cross-book projection bug: a correct JIUCHUI relationship transition rendered with the SHADOW book phrase “确认分影…同行”. Runs 1–3 and their blind results remain evidence of the failed v1 projection and are not eligible for the final quality/time claim.

Before v2, only the following root fixes were allowed: cross-book surface correction; punctuation normalization; natural rendering of multi-object clauses; fixed actor signature for the outer-pass reaction; deterministic fail-closed surface-scope checks. No model, effort, upstream Authority, downstream prompt, sample, control route or judge preference changed.

`e2e-run4` and `e2e-run5` must use the exact hashes in `PROTOCOL_FREEZE_V2.json`. No post-blind template tuning or cherry-picking is allowed. If either run falls back, its discarded Native Director wall and fallback Director wall remain in complete E2E.
